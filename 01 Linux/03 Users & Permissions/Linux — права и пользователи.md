## 9. Linux — права и пользователи

```bash
id
whoami
ls -l
```

Изменение владельца:
```bash
sudo chown user:group file
```

Права:
```bash
chmod 644 file
chmod +x script.sh
```

Запомнить:
- `r` = read
- `w` = write
- `x` = execute
